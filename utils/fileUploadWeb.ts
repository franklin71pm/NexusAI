// Utilidad mock para guardar archivos en web (no hace nada)
export async function saveFileToDiskWeb(folderPath: string, fileName: string, base64Data: string) {
  // No guardar en disco en web
  return true;
}
